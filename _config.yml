# This is the default format.
# For more see: http://jekyllrb.com/docs/permalinks/
permalink: /:categories/:year/:month/:day/:title

exclude: [".rvmrc", ".rbenv-version", "Rakefile", "vendor"]
pygments: true
redcarpet:
    extensions: ["tables", "autolink", "fenced_code_blocks", "superscript", "quote", "footnotes"]


# Themes are encouraged to use these universal variables
# so be sure to set them if your theme uses them.
#
title : Unai Zalakain
tagline:
author :
  name : Unai Zalakain
  email : unai@gisa-elkartea.org
  github : unaizalakain

production_url : http://unaizalakain.github.io

JB :
  version : 0.3.0
  BASE_PATH : false
  ASSET_PATH : /assets/
  atom_path : /atom.xml
  rss_path : /rss.xml
  comments :
    provider : false
  analytics :
    provider : false
  sharing :
    provider : false
